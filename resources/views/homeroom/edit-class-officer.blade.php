@extends('layout.layout')
@section('title', __('pages.edit_class_officer'))
@section('content')<section class="workspace-page"><form method="POST" action="{{ route('homeroom.class-officers.update',['id'=>$officer->id]) }}" class="card p-4">@csrf @method('PUT')<label class="form-label" for="position">{{ __('labels.position') }}</label><select id="position" name="position" class="form-select" required>@foreach(['class_president','vice_president','secretary','treasurer','class_officer'] as $position)<option value="{{ $position }}" @selected($officer->position===$position)>{{ __('positions.'.$position) }}</option>@endforeach</select><button class="btn btn-primary mt-4">{{ __('actions.save') }}</button><a href="{{ route('homeroom.class-officers.index') }}" class="btn btn-secondary mt-4">{{ __('actions.cancel') }}</a></form></section>@endsection
