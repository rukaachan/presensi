@extends('layout.layout')
@section('title', __('pages.class_officers'))
@section('page-actions')<a href="{{ route('homeroom.class-officers.create') }}" class="btn btn-primary">{{ __('actions.create') }}</a>@endsection
@section('content')<section class="workspace-page"><div class="table-scroll" tabindex="0"><table class="table table-bordered"><thead><tr><th>{{ __('labels.number') }}</th><th>{{ __('labels.student') }}</th><th>{{ __('labels.position') }}</th><th>{{ __('labels.actions') }}</th></tr></thead><tbody>@forelse($officers as $officer)<tr><td>{{ $loop->iteration }}</td><td>{{ $officer->student?->name }}</td><td>{{ __('positions.'.$officer->position) }}</td><td><a href="{{ route('homeroom.class-officers.show',['id'=>$officer->id]) }}">{{ __('actions.view') }}</a> · <a href="{{ route('homeroom.class-officers.edit',['id'=>$officer->id]) }}">{{ __('actions.edit') }}</a><button type="button" data-delete-url="{{ route('homeroom.class-officers.destroy') }}" data-delete-field="id" data-delete-id="{{ $officer->id }}">{{ __('actions.delete') }}</button></td></tr>@empty<tr><td colspan="4">{{ __('pages.no_class_officers') }}</td></tr>@endforelse</tbody></table></div>{{ $officers->links() }}</section>@endsection
