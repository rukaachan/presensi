<label class="form-label" for="name">{{ __('labels.name') }}</label><input id="name" name="name" class="form-control" value="{{ old('name',$teacher?->name) }}" required><label class="form-label mt-3" for="username">{{ __('labels.username') }}</label><input id="username" name="username" class="form-control" value="{{ old('username',$teacher?->account?->username) }}" required>@if(!$teacher)<label class="form-label mt-3" for="password">{{ __('labels.password') }}</label><input id="password" name="password" type="password" class="form-control" required><label class="form-label mt-3" for="role_code">{{ __('labels.role') }}</label><select id="role_code" name="role_code" class="form-select" required>@foreach(['homeroom_teacher','duty_teacher','counseling_teacher'] as $roleCode)<option value="{{ $roleCode }}">{{ __('roles.'.$roleCode) }}</option>@endforeach</select>@endif<label class="form-label mt-3" for="photo">{{ __('labels.photo') }}</label><input id="photo" name="photo" type="file" class="form-control" {{ $teacher ? '' : 'required' }}>
