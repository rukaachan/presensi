@extends('layout.layout')
@section('title', __('pages.teachers'))
@section('page-description', __('pages.teachers_description'))
@section('page-actions')<a href="{{ route('administration.teachers.create') }}" class="btn btn-primary page-action"><i class="ph-bold ph-plus" aria-hidden="true"></i> {{ __('actions.create') }}</a>@endsection
@section('content')<section class="workspace-page"><form action="{{ route('administration.teachers.index') }}" method="get" class="workspace-filters"><label class="search-field" for="keyword"><span class="sr-only">{{ __('actions.search') }}</span><input id="keyword" type="search" name="keyword" value="{{ request('keyword') }}" placeholder="{{ __('placeholders.search_teacher') }}"></label><button class="btn btn-secondary">{{ __('actions.apply') }}</button></form><div class="table-scroll" tabindex="0"><table class="table table-bordered"><thead><tr><th>{{ __('labels.number') }}</th><th>{{ __('labels.name') }}</th><th>{{ __('labels.username') }}</th><th>{{ __('labels.role') }}</th><th>{{ __('labels.actions') }}</th></tr></thead><tbody>@forelse($teachers as $teacher)<tr><td>{{ $teachers->firstItem()+$loop->index }}</td><td><strong>{{ $teacher->name }}</strong></td><td>{{ $teacher->account?->username }}</td><td>{{ $teacher->account?->role?->name }}</td><td class="table-actions"><a class="icon-action icon-action--info" href="{{ route('administration.teachers.show',['id'=>$teacher->id]) }}" aria-label="{{ __('actions.view') }}"><i class="ph-bold ph-arrow-up-right" aria-hidden="true"></i></a><a class="icon-action icon-action--edit" href="{{ route('administration.teachers.edit',['id'=>$teacher->id]) }}" aria-label="{{ __('actions.edit') }}"><i class="ph-bold ph-pencil-simple" aria-hidden="true"></i></a><button class="icon-action icon-action--danger" type="button" data-delete-url="{{ route('administration.teachers.destroy') }}" data-delete-field="id" data-delete-id="{{ $teacher->id }}" aria-label="{{ __('actions.delete') }}"><i class="ph-bold ph-trash" aria-hidden="true"></i></button></td></tr>@empty<tr><td colspan="5">{{ __('pages.no_teachers') }}</td></tr>@endforelse</tbody></table></div>{{ $teachers->links() }}</section>@endsection
