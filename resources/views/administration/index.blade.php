@extends('layout.layout')
@section('title', __('pages.administration_dashboard'))
@section('content')
<section class="dashboard-intro operations-intro" aria-labelledby="dashboard-title"><div><p class="eyebrow">{{ __('pages.today_operations') }}</p><h2 id="dashboard-title">{{ __('pages.administration_headline') }}</h2><p>{{ $operationalDate->locale('id')->isoFormat('dddd, D MMMM YYYY') }} · {{ __('pages.administration_description') }}</p></div><a class="quick-action rounded-md bg-accent text-accent-foreground ring-1 ring-accent" href="{{ route('administration.attendance.index') }}"><span>{{ __('nav.review_attendance') }}</span><i class="ph-bold ph-arrow-right" aria-hidden="true"></i></a></section>
<section class="daily-kpi-grid" aria-label="{{ __('pages.today_summary') }}"><article class="daily-kpi daily-kpi--primary"><div class="daily-kpi-heading"><span>{{ __('attendance.recorded') }}</span><i class="ph-bold ph-check-circle" aria-hidden="true"></i></div><strong>{{ $dailySummary['totalRecorded'] }} <small>/ {{ $dailySummary['totalActiveStudents'] }}</small></strong><div class="daily-kpi-progress" aria-hidden="true"><span style="width: {{ $dailySummary['completionRate'] }}%;"></span></div><p>{{ $dailySummary['completionRate'] }}% {{ __('pages.students_recorded') }}</p></article><article class="daily-kpi"><div class="daily-kpi-heading"><span>{{ __('attendance.missing') }}</span><i class="ph-bold ph-clock-countdown" aria-hidden="true"></i></div><strong>{{ $dailySummary['totalMissing'] }}</strong><p>{{ __('pages.students_missing') }}</p></article><article class="daily-kpi {{ $dailySummary['needsReview'] > 0 ? 'daily-kpi--attention' : '' }}"><div class="daily-kpi-heading"><span>{{ __('attendance.needs_review') }}</span><i class="ph-bold ph-warning-circle" aria-hidden="true"></i></div><strong>{{ $dailySummary['needsReview'] }}</strong><p>{{ __('pages.review_description') }}</p></article><article class="daily-kpi"><div class="daily-kpi-heading"><span>{{ __('pages.complete_classrooms') }}</span><i class="ph-bold ph-chalkboard" aria-hidden="true"></i></div><strong>{{ $dailySummary['classesComplete'] }} <small>/ {{ $dailySummary['totalActiveClasses'] }}</small></strong><p>{{ __('pages.complete_classrooms_description') }}</p></article></section>
<div class="operations-grid"><section class="operations-panel" aria-labelledby="class-readiness-title"><header class="operations-panel-heading"><div><h3 id="class-readiness-title">{{ __('pages.classroom_readiness') }}</h3><p>{{ __('pages.classroom_readiness_description') }}</p></div><a href="{{ route('administration.attendance.index') }}">{{ __('actions.view_all') }} <i class="ph-bold ph-arrow-up-right" aria-hidden="true"></i></a></header>@forelse($classReadiness as $class)<a class="class-readiness-row" href="{{ route('administration.attendance.index',['classroom_id'=>$class->id]) }}"><div class="class-readiness-copy"><strong>{{ $class->grade_level }} {{ $class->department_name }} {{ $class->classroom_name }}</strong><span>{{ $class->total_recorded }} dari {{ $class->total_students }} {{ __('pages.students_recorded_short') }}</span></div><div class="class-readiness-progress" aria-hidden="true"><span style="width: {{ $class->completionRate }}%;"></span></div><span class="class-readiness-status {{ $class->isComplete ? 'is-complete' : '' }}">{{ $class->isComplete ? __('attendance.complete') : $class->completionRate.'%' }}</span></a>@empty<div class="operations-empty-state"><i class="ph-bold ph-chalkboard" aria-hidden="true"></i><strong>{{ __('pages.no_active_classrooms') }}</strong><p>{{ __('pages.no_active_classrooms_description') }}</p></div>@endforelse</section>
<aside class="attention-panel" aria-labelledby="attention-title"><header class="operations-panel-heading"><div><h3 id="attention-title">{{ __('attendance.needs_attention') }}</h3><p>{{ __('pages.follow_up_priority') }}</p></div></header><a class="attention-item" href="{{ route('administration.attendance.index',['state'=>'absent']) }}"><span class="attention-item-icon attention-item-icon--danger"><i class="ph-bold ph-user-minus" aria-hidden="true"></i></span><span><strong>{{ $dailySummary['totalAbsent'] }} {{ __('attendance.absent') }}</strong><small>{{ __('pages.absent_follow_up') }}</small></span><i class="ph-bold ph-caret-right" aria-hidden="true"></i></a><a class="attention-item" href="{{ route('administration.attendance.index',['state'=>'needs_review']) }}"><span class="attention-item-icon"><i class="ph-bold ph-seal-question" aria-hidden="true"></i></span><span><strong>{{ $dailySummary['pendingValidation'] }} {{ __('attendance.needs_review') }}</strong><small>{{ __('pages.review_follow_up') }}</small></span><i class="ph-bold ph-caret-right" aria-hidden="true"></i></a><a class="attention-item" href="{{ route('administration.classrooms.index',['status'=>'active']) }}"><span class="attention-item-icon"><i class="ph-bold ph-list-checks" aria-hidden="true"></i></span><span><strong>{{ max(0,$dailySummary['totalActiveClasses']-$dailySummary['classesComplete']) }} {{ __('pages.incomplete_classrooms') }}</strong><small>{{ __('pages.incomplete_classrooms_description') }}</small></span><i class="ph-bold ph-caret-right" aria-hidden="true"></i></a></aside></div>
<section class="activity-panel" aria-labelledby="recent-activity-title"><header class="operations-panel-heading"><div><h3 id="recent-activity-title">{{ __('pages.recent_activity') }}</h3><p>{{ __('pages.recent_activity_description') }}</p></div><a href="{{ route('administration.audits.index') }}">{{ __('nav.audits') }} <i class="ph-bold ph-arrow-up-right" aria-hidden="true"></i></a></header><div class="activity-list">@forelse($recentAudits as $audit)<div class="activity-item"><span class="activity-item-mark"><i class="ph-bold ph-activity" aria-hidden="true"></i></span><div><strong>{{ $audit->actor_type ? __('audit.actors.'.$audit->actor_type) : __('labels.system') }} · {{ __('audit.actions.'.$audit->action) }}</strong><span>{{ __('audit.subjects.'.($audit->subject_type ?: 'system_event')) }}</span></div><time datetime="{{ $audit->occurred_at }}">{{ $audit->occurred_at }}</time></div>@empty<div class="operations-empty-state operations-empty-state--compact"><i class="ph-bold ph-notebook" aria-hidden="true"></i><strong>{{ __('pages.no_recent_activity') }}</strong><p>{{ __('pages.no_recent_activity_description') }}</p></div>@endforelse</div></section>
@endsection
