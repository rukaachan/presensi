@extends('layout.layout')
@section('title', __('pages.class_officers'))
@section('page-description', __('pages.class_officers_description'))
@section('page-actions')<a href="{{ route('administration.class-officers.create') }}" class="btn btn-primary page-action"><i class="ph-bold ph-plus" aria-hidden="true"></i> {{ __('actions.create') }}</a>@endsection
@section('content')<section class="workspace-page"><form action="{{ route('administration.class-officers.index') }}" method="get" class="workspace-filters"><label class="search-field" for="keyword"><span class="sr-only">{{ __('actions.search') }}</span><input id="keyword" type="search" name="keyword" value="{{ request('keyword') }}" placeholder="{{ __('placeholders.search_student') }}"></label><button class="btn btn-secondary">{{ __('actions.apply') }}</button></form><div class="table-scroll" tabindex="0"><table class="table table-bordered"><thead><tr><th>{{ __('labels.number') }}</th><th>{{ __('labels.name') }}</th><th>{{ __('labels.classroom') }}</th><th>{{ __('labels.position') }}</th><th>{{ __('labels.actions') }}</th></tr></thead><tbody>@forelse($officers as $officer)<tr><td>{{ $officers->firstItem()+$loop->index }}</td><td><strong>{{ $officer->student?->name }}</strong></td><td>{{ $officer->student?->classroom?->name }}</td><td>{{ __('positions.'.$officer->position) }}</td><td class="table-actions"><a class="icon-action icon-action--info" href="{{ route('administration.class-officers.show',['id'=>$officer->id]) }}" aria-label="{{ __('actions.view') }}"><i class="ph-bold ph-arrow-up-right" aria-hidden="true"></i></a><a class="icon-action icon-action--edit" href="{{ route('administration.class-officers.edit',['id'=>$officer->id]) }}" aria-label="{{ __('actions.edit') }}"><i class="ph-bold ph-pencil-simple" aria-hidden="true"></i></a><button class="icon-action icon-action--danger" type="button" data-delete-url="{{ route('administration.class-officers.destroy') }}" data-delete-field="id" data-delete-id="{{ $officer->id }}" aria-label="{{ __('actions.delete') }}"><i class="ph-bold ph-trash" aria-hidden="true"></i></button></td></tr>@empty<tr><td colspan="5">{{ __('pages.no_class_officers') }}</td></tr>@endforelse</tbody></table></div>{{ $officers->links() }}</section>@endsection
