@extends('layout.layout')
@section('title', __('pages.create_class_officer'))
@section('content')<section class="workspace-page"><form method="POST" action="{{ route('administration.class-officers.store') }}" class="card p-4">@csrf<label class="form-label" for="student_id">{{ __('labels.student') }}</label><select id="student_id" name="student_id" class="form-select" required>@foreach($students as $student)<option value="{{ $student->id }}">{{ $student->name }} — {{ $student->classroom?->name }}</option>@endforeach</select><label class="form-label mt-3" for="position">{{ __('labels.position') }}</label><select id="position" name="position" class="form-select" required>@foreach(['class_president','vice_president','secretary','treasurer'] as $position)<option value="{{ $position }}">{{ __('positions.'.$position) }}</option>@endforeach</select><button class="btn btn-primary mt-4">{{ __('actions.save') }}</button><a href="{{ route('administration.class-officers.index') }}" class="btn btn-secondary mt-4">{{ __('actions.cancel') }}</a></form></section>@endsection
