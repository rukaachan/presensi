@extends('layout.layout')
@section('title', __('pages.departments'))
@section('page-description', __('pages.departments_description'))
@section('page-actions')<a href="{{ route('administration.departments.create') }}" class="btn btn-primary page-action"><i class="ph-bold ph-plus" aria-hidden="true"></i> {{ __('actions.create') }}</a>@endsection
@section('content')<section class="workspace-page"><form action="{{ route('administration.departments.index') }}" method="get" class="workspace-filters"><label class="search-field" for="keyword"><span class="sr-only">{{ __('actions.search') }}</span><input id="keyword" type="search" name="keyword" value="{{ request('keyword') }}" placeholder="{{ __('placeholders.search_department') }}"></label><button class="btn btn-secondary" type="submit">{{ __('actions.apply') }}</button></form><div class="table-scroll" tabindex="0"><table class="table table-bordered"><thead><tr><th>{{ __('labels.number') }}</th><th>{{ __('labels.name') }}</th><th>{{ __('labels.actions') }}</th></tr></thead><tbody>@forelse($departments as $department)<tr><td>{{ $departments->firstItem()+$loop->index }}</td><td><strong class="table-primary-text">{{ $department->name }}</strong></td><td class="table-actions"><a class="icon-action icon-action--edit" href="{{ route('administration.departments.edit',['id'=>$department->id]) }}" aria-label="{{ __('actions.edit') }}"><i class="ph-bold ph-pencil-simple" aria-hidden="true"></i></a><button class="icon-action icon-action--danger" type="button" data-delete-url="{{ route('administration.departments.destroy') }}" data-delete-field="id" data-delete-id="{{ $department->id }}" aria-label="{{ __('actions.delete') }}"><i class="ph-bold ph-trash" aria-hidden="true"></i></button></td></tr>@empty<tr><td colspan="3">{{ __('pages.no_departments') }}</td></tr>@endforelse</tbody></table></div>{{ $departments->links() }}</section>@endsection
