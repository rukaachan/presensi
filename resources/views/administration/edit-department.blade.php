@extends('layout.layout')
@section('title', __('pages.edit_department'))
@section('content')<section class="workspace-page"><form method="POST" action="{{ route('administration.departments.update',['id'=>$department->id]) }}" class="card p-4">@csrf @method('PUT')<label for="name" class="form-label">{{ __('labels.name') }}</label><input id="name" name="name" class="form-control" value="{{ old('name',$department->name) }}" required>@error('name')<div class="invalid-feedback d-block">{{ $message }}</div>@enderror<div class="mt-4"><button class="btn btn-primary">{{ __('actions.save') }}</button><a href="{{ route('administration.departments.index') }}" class="btn btn-secondary">{{ __('actions.cancel') }}</a></div></form></section>@endsection
