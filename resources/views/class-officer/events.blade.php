@extends('layout.layout')
@section('title', __('pages.optional_events'))
@section('page-description', __('pages.optional_events_description'))
@section('content')<section class="workspace-page"><form method="POST" action="{{ route('class-officer.events.suggest') }}" class="card p-4 mb-4">@csrf<div class="row g-3"><div class="col-md-4"><label class="form-label" for="student_id">{{ __('labels.student') }}</label><select id="student_id" name="student_id" class="form-select" required>@foreach($students as $student)<option value="{{ $student->id }}">{{ $student->name }}</option>@endforeach</select></div><div class="col-md-3"><label class="form-label" for="session_code">{{ __('labels.session') }}</label><select id="session_code" name="session_code" class="form-select" required>@foreach($sessions as $session)<option value="{{ $session->code }}">{{ __($session->label) }}</option>@endforeach</select></div><div class="col-md-3"><label class="form-label" for="proposed_status">{{ __('labels.suggested_status') }}</label><select id="proposed_status" name="proposed_status" class="form-select" required>@foreach(['confirmed','excused','absent','checked_out'] as $status)<option value="{{ $status }}">{{ __('attendance.'.$status) }}</option>@endforeach</select></div><div class="col-md-2"><label class="form-label" for="event_date">{{ __('labels.date') }}</label><input id="event_date" type="date" name="event_date" class="form-control" value="{{ now((string)config('attendance.timezone'))->toDateString() }}"></div></div><button class="btn btn-primary mt-3">{{ __('actions.submit_suggestion') }}</button></form><div class="table-scroll" tabindex="0"><table class="table table-bordered"><thead><tr><th>{{ __('labels.number') }}</th><th>{{ __('labels.student') }}</th><th>{{ __('labels.session') }}</th><th>{{ __('labels.date') }}</th><th>{{ __('labels.status') }}</th><th>{{ __('labels.suggested_status') }}</th></tr></thead><tbody>@forelse($events as $event)<tr><td>{{ $events->firstItem()+$loop->index }}</td><td>{{ $event->student?->name }}</td><td>{{ $event->session?->label ? __($event->session->label) : '—' }}</td><td>{{ optional($event->event_date)->format('d/m/Y') }}</td><td>{{ __('attendance.'.$event->state->value) }}</td><td>{{ $event->proposed_status ? __('attendance.'.$event->proposed_status) : '—' }}</td></tr>@empty<tr><td colspan="6">{{ __('pages.no_events') }}</td></tr>@endforelse</tbody></table></div>{{ $events->links() }}</section>@endsection
